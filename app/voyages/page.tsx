import {voyages} from "@/lib/data";export const metadata={title:"Voyages"};export default function Page(){return <main className="shell"><header className="pagehead"><div className="ey">Ships · routes · arrivals</div><h1>Voyages</h1><p className="lead">Migration records connect individual lives to ships, ports, dates and destinations, revealing the routes, movements and historical circumstances that shaped journeys.</p></header><section className="section"><div className="grid2">{voyages.map(v=><article className="card" key={v.ship+v.arrival}><span className="tag">{v.source}</span><h3>{v.ship}</h3><p><b>{v.route}</b><br/>{v.departure} → {v.arrival}</p><p>{v.context}</p></article>)}</div></section></main>}
